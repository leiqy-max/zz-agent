# 服务器故障排查指南

## 1. 无法连接服务器
如果出现无法连接服务器的情况，请按以下步骤排查：
1. 检查网络连接是否正常 (ping IP)。
2. 检查 SSH 服务是否启动 (systemctl status sshd)。
3. 检查防火墙规则 (iptables -L)。

## 2. 磁盘空间不足
当磁盘空间告警时：
1. 使用 `df -h` 查看挂载点使用率。
2. 使用 `du -sh *` 查找大文件。
3. 清理 `/var/log` 下的旧日志文件。
